{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default AgentOps desktop window capability. Sensitive filesystem, terminal and patch operations are handled by guarded project-scoped commands.",
  "windows": ["main"],
  "permissions": []
}
